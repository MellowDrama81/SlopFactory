# Build and test

## Prerequisites

- .NET SDK 10.0.302 or a compatible latest patch selected by `global.json`
- .NET MAUI Windows and Android workloads
- Windows SDK for the Windows target
- Android SDK for the Android target

## Restore

```powershell
dotnet restore SlopFactory.slnx --configfile NuGet.Config
```

## Tests

```powershell
dotnet test tests\Mellow.SlopFactory.Tests\Mellow.SlopFactory.Tests.csproj --no-restore
```

The current 296 tests cover platform-version policy, initialization, manifest/database creation, copied-library adoption, local-storage path rejection, hard-link rejection, and open-library identity revalidation, exclusive locking, invalid library entries, recursive import inventory and frozen-source revalidation, primary-stream and zone handling, verified single/bulk/recovery export, external-open isolation, resumable integrity checkpoints, cross-library review isolation, safe managed import, hashing, progress, cancellation cleanup, single and bulk duplicate handling with direct provenance chains and deletion snapshots, folder/file organization, reviewed multi-file operations, bulk metadata sensitivity and type normalization, typed metadata ownership and filtering, redacted JSON validation, bounded media probing, privacy-minimised integrity-report serialization, responsive/focus UI assets, dialog focus restoration (including that every appear/disappear confirmation panel across `FileDetails`, `RecycleBin`, `Home`, `LibrarySettings`, `Connections`, `Models`, `SavedSettings`, `Generate`, `GenerationHistory` and `GenerationHistoryDetail` actually uses `role="dialog"` rather than `role="group"`, so the shared `ui.js` focus-restoration helper fires for all of them, not just the two pages it was originally added for), and localization-resource wiring.

They also cover detected-media-type viewer allow-listing and preview-unavailable handling, bounded text, Markdown, raster-image (including temporary JPEG EXIF orientation with unchanged managed bytes and hash), SVG and media viewing; managed-content health, safe changed-byte inspection, and replacement; editable links; aggregate recycling, restoration and retryable permanent deletion; coherent integrity scans; culture-specific UI-resource fixtures; and version 1 through version 5, and version 14 through version 24, upgrades to the current schema (v25) with rollback cleanup. Preview-cache rebuilding is verified through the Windows and Android application builds.

They also cover connection and model validation, label uniqueness, recycle/restore/permanent-delete cascades between connections and their dependent models, model-catalogue refresh persisting discovered entries with a retrieval timestamp, a failed refresh marking the retained catalogue possibly-stale without clearing it or its timestamp, connection timeout override validation/persistence independent of test status, additional-connection-header validation (count/length/duplicate/reserved-name/credential-header rejection) and persistence, generic-connection per-modality settings defaulting to all-enabled/no-override plus relative-path-override validation, provider-type changes being rejected while active dependent models exist but succeeding (and resetting generic-modality settings) once none remain, and a model's provider-model-ID/mode change marking it and its active saved settings **Needs Review** (a label-only edit leaves the flag untouched) with `MarkModelReviewedAsync` clearing both (`ConnectionModelTests`), version 14 through version 24 upgrades to the current schema (v25) adding the model-catalogue cache, connection-timeout column, connection-headers table, per-modality settings columns, the prompt-improvement history table, the needs-review columns, the per-model/per-generation text-format columns, the `generation_drafts` table, the `connection_credential_revisions` ledger, the saved-setting `revision` column, and the `generation_results.file_id` `ON DELETE CASCADE` fix with rollback cleanup — the version-20 upgrade also verifies a model updated to the plain-text format actually commits a `.txt`/`text/plain` result and the generation record snapshots that format, the version-21 upgrade verifies a draft created after the upgrade defaults its destination folder from the descriptor and round-trips a custom title and prompt through `ReplaceDraftStateAsync`, the version-22 upgrade verifies a connection's credential can be staged and promoted through a fresh ledger after the upgrade, the version-23 upgrade verifies a saved setting reloaded after the upgrade starts at revision 1 and increments to 2 on its first post-upgrade update, and the version-24 upgrade (reproducing the pre-fix schema directly) verifies that permanently deleting a generation-result file no longer throws and instead clears just that file from the record's `ResultFileIds` (`LibraryWorkspaceTests`), and the OpenAI and generic OpenAI-compatible provider adapters' connection-test, model-listing, chat-completion text-generation and images/generations behavior against a fake `HttpMessageHandler`, including authentication failure, an unreachable host, a missing model-listing endpoint, rate limiting, multi-candidate parsing for both text and images, a configured timeout throwing `ProviderAdapterException` distinctly from a bare cancellation, real caller-driven cancellation during a timed request still propagating as `TaskCanceledException`, additional headers being sent alongside the credential header, a per-modality relative-path override being used to build the request URL, a disabled modality throwing `ProviderAdapterException` without the fake handler ever being invoked, model listing transparently retrying once after a single `429` and succeeding, model listing giving up after a small bounded number of attempts when every response is `429`, and a text-generation request that receives a `429` never retrying automatically (the fake handler is invoked exactly once) (`ProviderAdapterTests`, using a delay-aware `FakeHttpMessageHandler` overload that observes the request's cancellation token, and forcing `Retry-After: 0` on fake `429` responses so bounded-retry tests resolve near-instantly). `GenerationRecordTests` cover committing generated text and images (including real file-signature media-type detection for a PNG payload) as managed files linked to a generation-history record, the no-files/sanitized-error path for a failed attempt of either mode, the 1 MiB UTF-8 bound on the prompt and system instructions (an over-limit value is rejected, a value exactly at the limit succeeds), and a text or image generation that commits fewer results than requested landing on **Partially Completed** rather than **Completed**. Two crash-injection tests, mirroring the Milestone 1 export crash-injection technique of forcing an interruption at a specific boundary and asserting on the recovery state rather than trusting it: `PartiallyCommittedTextGenerationLeavesTheEarlierResultFileIntactWithNoOrphanedHistoryRecord` forces a second result string containing an unpaired surrogate to throw only after the first result file has already been staged, hashed, moved and inserted, then verifies that first file is a perfectly healthy active Generated file with no `.generating` staging leftover and that no `GenerationRecord` was created at all for the attempt; `CancelledImageGenerationCommitLeavesNoOrphanedStagingFileOrHistoryRecord` covers the same commit pipeline's other boundary with a pre-cancelled token. `PermanentlyDeletingAGenerationResultFileClearsItsResultReferenceWithoutRemovingTheHistoryRecord` is a regression test for a genuine bug found while scoping the generation-history recycle-bin item: permanently deleting a generation-result file used to unconditionally throw (a missing `ON DELETE` clause on `generation_results.file_id`); it now clears just that one result reference while the record, its other results, and its status/count all stay untouched. `SavedGenerationSettingTests` and `PromptImprovementRecordTests` verify the same bound on saved-setting prompt/system-instructions and prompt-improvement raw-prompt/guidance respectively. `SavedGenerationSettingTests` cover title uniqueness, model snapshotting, cascade recycle/restore/permanent-delete of saved settings from both their owning model and, through it, their owning connection, persisting/clearing system instructions, a freshly created saved setting starting at revision 1, updating with a stale revision throwing `SavedSettingRevisionConflictException` carrying the current authoritative record without writing anything, and overwriting after a conflict succeeding once retried with that exception's fresh current revision. `ProviderAdapterTests` also verify the chat-completion request includes a `system` message only when system instructions are supplied, that reported prompt/completion token usage is parsed when present and absent otherwise, and that supplying a source image switches the user message to the OpenAI vision multi-part content shape with a correctly encoded data URI. `GenerationRecordTests` and `SavedGenerationSettingTests` verify system instructions, token usage and a source-file reference all persist and reload correctly, including that permanently deleting the referenced source file clears the reference on existing generation history rather than leaving it dangling. `PromptImprovementRecordTests` cover a successful attempt persisting its candidates and token usage, a failed attempt followed by a retry producing two separate records rather than one being overwritten, an accepted attempt's link surviving permanent deletion of the improvement model (the model reference clears to `null` on the improvement record itself, but the generation record's link to that improvement record is untouched), and the same 1 MiB UTF-8 bound applying to the raw prompt, guidance, and each individual returned candidate. `GenerationDraftTests` cover `CreateDraftAsync` defaulting its destination folder and result count, `ReplaceDraftStateAsync` round-tripping every field (including clearing a custom title back to the automatic-title `null` sentinel), `DuplicateDraftAsync` copying every field except the title and landing adjacent in tab order (renumbering trailing siblings), `DeleteDraftAsync` removing a draft and throwing `RecordNotFoundException` on a second call, permanently deleting a referenced model or file clearing the draft's reference to `null` rather than throwing, `ReorderDraftsAsync` rewriting every draft's `tab_order` from a supplied full ordered-ID list and rejecting both a wrong-count and a same-count-but-mismatched-ID set with `LibraryValidationException`, and the same 1 MiB UTF-8 bound applying to a draft's prompt, system instructions and prompt-improvement guidance. `GenerationQueueServiceTests` link `GenerationQueueService.cs` and `ISecureCredentialStore.cs` directly into this plain xUnit project (the same `<Compile Include>` convention already used for `AppLibraryState.cs` and friends) and exercise the real scheduler against a real `LibraryWorkspaceFactory`-created library, a `TaskCompletionSource`-gated fake `IProviderAdapter`, and a fake credential store: FIFO order within one connection, per-connection concurrency never exceeding one even with many jobs queued, the device-wide cap (3, exercised on the Windows test runner the suite actually runs on — the Android value of 2 is only a source-level constant from this suite's perspective) capping total running across several connections, round-robin giving a second connection's job a turn rather than letting a backlogged connection start both available slots itself, the rotation cursor correctly picking up a connection's new arrival after its own queue had emptied, cancelling a queued job never invoking the adapter and creating no `GenerationRecord`, cancelling a running job triggering its token and also creating no record, a successful job committing a real `GenerationRecord` and remaining the draft's last-known outcome until a later run supersedes it, enqueueing twice for the same draft while a job is already active being idempotent, a job whose model was recycled and permanently deleted while it sat queued failing locally without fabricating a record, and switching the active library dropping queued jobs and cancelling running ones tied to the outgoing workspace without an unhandled exception, `GetSnapshot()` reporting each non-terminal job's phase and queue position across connections, and `ReorderQueuedJobs` rewriting a connection's queued order from a supplied ID list while rejecting both a wrong-count and a same-count-but-mismatched set with `InvalidOperationException`. The same suite covers the adjustable device-wide submission cap: `SetDeviceCap` clamping a value outside `MinDeviceCap`/`MaxDeviceCap` to that range, the cap persisting across a second `GenerationQueueService` instance constructed against the same `IAppPreferenceStore` (simulating a restart), and raising the cap while a job is already waiting behind a full device immediately starting it rather than waiting for a running job to finish first. It also covers the energy-saver-driven temporary cap using a stateful `FakeDeviceEnergyStateProvider`: `EnergySaverCapActive`/`EffectiveDeviceCap` reflecting the provider's current state, enabling energy-saver mode while three jobs already run leaving all three running untouched while a fourth stays queued, completing one of the three running jobs still not releasing the queued fourth while energy-saver remains on, and disabling energy-saver then immediately starting that fourth job without any further trigger. `CredentialRevisionLifecycleTests` exercise the credential ledger directly against a real `LibraryWorkspaceFactory`-created library: staging a candidate, promoting it (flipping its ledger purpose and updating the connection's pointer), promoting a second revision superseding and dropping the first from the ledger, promoting an unknown revision throwing `RecordNotFoundException`, discarding a candidate without touching an already-promoted revision, `MarkCredentialRequiresRepairAsync` setting the flag without touching the pointer or ledger, deleting a specific ledger row, and permanently deleting a connection cascading its ledger rows. `CredentialReconciliationServiceTests` link `CredentialReconciliationService.cs` into the same plain xUnit project and, using a stateful fake credential store (tracking real per-key active/candidate/legacy values, unlike the fixed-string fake `GenerationQueueServiceTests` uses), verify an orphaned candidate found at startup is swept from both the ledger and secure storage, a committed pointer with no matching active ledger row is flagged **Requires Repair** without deleting anything, a committed pointer whose active secure-storage value has gone missing is likewise flagged, a stray superseded active ledger row under an otherwise-valid pointer is cleaned up from both the ledger and secure storage while the valid revision is untouched, a pre-existing connection's legacy (non-revisioned) credential is silently adopted into revision 1 with its existing test status preserved exactly, and a legacy `HasCredential` connection whose legacy secure-storage key has already vanished is flagged **Requires Repair** instead of adopting nothing. `AppLibraryState`'s new `Closing` pre-close event is covered directly in `LibraryWorkspaceTests`: it fires exactly once immediately before a library switch while the outgoing workspace is still valid (so a subscriber reading `Workspace` inside the handler still sees the *outgoing* library, not the new one), does not fire on the very first switch when there was no previous workspace to flush, fires before `CloseInvalidLibraryAsync` clears `Workspace`, and deliberately does not fire before `CloseUnavailableLibraryAsync` (a flush attempt against confirmed-unreachable storage could only add a doomed wait). The unified recycle bin's extension to connections, models and saved settings is covered directly in `LibraryWorkspaceTests`: a recycled connection appears as one entry with correct owned model/saved-setting counts while its models and their saved settings are hidden rather than double-listed, a model recycled on its own (connection left active) appears independently with the connection's label as its original location, a saved setting with no model reference (`ModelId: null`) still appears rather than becoming unreachable, active-label/title conflicts block a connection/model/saved-setting restore the same way they already do for folders/files, a saved setting restore reports no blocker at all when its owning model is active (proving the restore-blocker logic does *not* mirror File/Folder's parent-must-be-active check for this kind, since the underlying `RestoreSavedSettingAsync` doesn't enforce one either), batch restore/`EmptyRecycleBinAsync` correctly handle a mix of a cascading connection and a standalone model together, and permanently deleting a connection through the bin cascades to its model and saved setting exactly as the direct API already does. The Connections, Models, Generate, generation-history (list and detail), Queue, and saved-settings pages have the same localization-guard coverage as other pages, verifying every application-owned UI string is resolved through `IStringLocalizer<UiStrings>` rather than hard-coded, including the generation-history **Use Again** link, its status/mode/model/provider/date-range filters, its **View Details** link to the new `/generation-history/{Id}` detail page (which carries its own separate guard covering the prompt/system-instructions/error/usage/result-link content that moved off the list), the Generate page's third `/generate/history/{HistoryId}` route, its **Cancel** action, its **Improve Prompt** panel, its tab strip (rename, reset-to-automatic-title, duplicate, move-left/move-right, and the three-way discard/save-settings-first/keep-open close dialog), **Saving**/**Saved**/**Not Saved**/**Retry Save** autosave status, and the **Queued**/queue-position/**Cancel** text now driving `GenerationQueueService` state, the **Credentials Required**/**Unverified**/**Credential State Requires Repair** connection-status text and the **Keep Existing Key**/**Save New Key as Unverified** decision-panel text on both the Connections list, `ConnectionEdit`, and `/generate`, the **Save**/**Save As**/**Overwrite** saved-settings actions and conflict-panel text, the per-model system-instructions capability note, the insecure-HTTP base-URL warning on both the Connections list and `ConnectionEdit`, the **Needs Review** badges/confirmation flow across `ModelEdit`, `Models`, `SavedSettings` and `Generate`, `MainLayout.razor`'s aggregate queued/running activity notice and its `NavQueue` link, and the `/queue` page's per-connection grouping, move-left/move-right reorder controls and **Cancel** action. The cancellation flow, the prompt-improvement round trip, the credential/verification status gating, the system-instructions capability gating, the live HTTP base-URL warning, the tab-strip's actual click-driven switching/debounce/close-confirmation interactions, live tab-strip/queue-notice rendering and page-navigate-away-and-back behavior in the real MAUI shell, and a draft tab's fallback/notice behavior when its stored model becomes recycled, permanently deleted or newly marked **Needs Review** (`LoadDraftIntoForm`'s `DraftModelUnavailable` path) are all UI-interaction behavior not covered by an automated test; they are candidates for the manual test matrix. `LibraryRules.NormalizeConnectionBaseUrl`'s underlying validation (HTTPS-required, loopback/private HTTP allowance, embedded-credential rejection) is covered by `ConnectionModelTests`. `GenerationQueueServiceTests` also cover the new `JobCompleted` event firing exactly once per finished job across the success, cancellation and local-pre-submission-failure paths, with the correct outcome payload for each. `AppLifecycleStateTests` cover `AppLifecycleState.SetForeground` toggling `IsForeground` and raising `Changed` only on an actual state transition, not on a redundant same-value call. `GenerationNotificationCoordinatorTests` link `GenerationNotificationCoordinator.cs`, `IAppLifecycleState.cs` and `INotificationService.cs` into the same plain xUnit project and exercise the coordinator against a real `GenerationQueueService` (driven the same way `GenerationQueueServiceTests` does) plus fakes of `INotificationService`/`IAppPreferenceStore`: `SetEnabledAsync(true)` requesting permission and persisting the setting only when granted, `SetEnabledAsync(false)` never requesting permission, a backgrounded completed job raising `NotifyRequested` with the real `GenerationRecord`, a completed job while foregrounded or while the setting is disabled never raising it, a completed job whose record's page is marked as the currently-visible one never raising it (proven by subscribing the test's own `JobCompleted` handler before `Coordinator.Start()`, so it learns and marks the freshly minted record ID as visible before the coordinator's own handler runs for that same event dispatch), and a local pre-submission failure (no `GenerationRecord` ever created) never raising it either. `MauiNotificationService`'s actual Windows (`Microsoft.Windows.AppNotifications`) and Android (`NotificationCompat`/`NotificationManagerCompat`) bodies are real MAUI/platform-SDK code with no test harness in this repo (same convention as `MauiSecureCredentialStore`/`MauiDeviceEnergyStateProvider`) — covered only by the Windows/Android application builds and the manual test matrix. `Generate.razor`'s save-conflict **Review Changes** field-level diff (`BuildConflictDiffRows`) is razor-page-only logic with no bUnit harness in this repo (same convention as the rest of the file's UI-interaction behavior); `UiAssetTests.GenerateUsesLocalizedResourcesForAllApplicationOwnedUiText` was extended to guard its new localization keys, and its actual diff behavior is a manual test matrix candidate. The Android compact tab-switcher and searchable tab-management list are the same kind of click-driven, razor-page-only logic with no automated behavioral coverage; a new `UiAssetTests.GenerateOffersAnAndroidCompactTabSwitcherWithASearchableManagementList` guards the structural facts a text-based test *can* check (the `OperatingSystem.IsAndroid()` branch, `<Virtualize>` usage, the generalized per-draft `DuplicateDraftAsync(draft.Id)`/`BeginSwitcherRename(draft)`/`MoveDraftAsync(draft.Id,`/`BeginCloseDraft(draft.Id)` call sites, and the strip's `overflow-x: auto` CSS rule) — mirroring the same text-assertion style already used for `AndroidManifestAndStorageGuidanceExcludeBackupAndBroadPermissions` and similar. Actually exercising the switcher (filtering, rename/duplicate/reorder/close on a non-active tab, virtualized scrolling) is a manual test matrix candidate. `SavedGenerationSettingTests` gained coverage for the saved-settings source-dependency-restoration domain sequence: `ARecycledSavedSettingCanBeRestoredAndImmediatelyUpdatedWithNewFieldsInOneFlow` verifies restoring a recycled saved setting and immediately updating it with the tab's current values in one flow succeeds and increments its revision; `RestoringARecycledSavedSettingWhoseTitleNowCollidesWithAnActiveOneThrowsANameConflict` verifies a title collision at restore time surfaces as `NameConflictException`; `GetSavedSettingThrowsRecordNotFoundAfterPermanentDeletionSoASourceTabCanDetectItWasDeletedRatherThanJustRecycled` verifies both `GetSavedSettingAsync` and `RestoreSavedSettingAsync` throw `RecordNotFoundException` once a saved setting is gone for good, which is exactly the signal `Generate.razor`'s `SaveInPlaceAsync` uses to distinguish "permanently deleted" from "merely recycled". The actual `Generate.razor` UI wiring for this (the new recycled-conflict panel, clearing `_loadedSettingsId` on deletion) has no bUnit harness in this repo and is a manual test matrix candidate, same as every other tab-strip/save-conflict interaction in this file; `UiAssetTests.GenerateUsesLocalizedResourcesForAllApplicationOwnedUiText` was extended to guard its four new localization keys. `ConnectionModelTests` gained the equivalent domain-sequence coverage for a draft's own model dependency: `ARecycledModelCanBeFetchedByIdWithItsLabelIntactAndRestoredDirectlyWithoutRecyclingItsConnection` verifies `GetModelAsync` returns a recycled model with its label intact (the exact signal `Generate.razor`'s `LoadDraftIntoFormAsync` reads to distinguish "recycled, offer restore" from "permanently deleted, must replace"), and that `RestoreModelAsync` succeeds directly without needing the owning connection to be recycled too; `RestoringARecycledModelWhoseLabelNowCollidesWithAnActiveOneThrowsANameConflict` verifies the same label-collision restore-conflict class the saved-settings-source restore flow already has to handle. `Generate.razor`'s own UI wiring (the recycled-model notice/restore button, the empty-selection block on permanent deletion) is, again, razor-page-only logic with no bUnit coverage — a manual test matrix candidate; `UiAssetTests.GenerateUsesLocalizedResourcesForAllApplicationOwnedUiText` was extended to guard its four new keys. `GenerationRecordTests` gained coverage for the generation-history recycle-bin and tombstoning slice: `RecyclingRestoringAndPermanentlyDeletingAGenerationRecordNeverTouchesItsFiles` verifies the full recycle/restore/permanent-delete lifecycle leaves the actual result file fully active throughout and confirms the record itself drops off/reappears on `GetGenerationHistoryAsync` correctly; `OnlyARecycledGenerationRecordCanBeRestoredOrPermanentlyDeleted` covers the state-guard rejections; `PermanentlyDeletingAGenerationResultFileClearsItsResultReferenceWithoutRemovingTheHistoryRecord` (the existing v25 regression test) was extended to also assert the now-tombstoned entry's display name/media type/content hash; `RecordingATextGenerationWithASourceImagePersistsAndClearsOnPermanentDeletion` was similarly extended for the source-file side. `LibraryWorkspaceTests` gained `RecycleBinIntegratesGenerationRecordsForListingBatchRestoreAndPermanentDelete` (the new kind appearing in the bin with the right name/original-location, batch restore, and permanent deletion through the bin leaving referenced files untouched) and a new `OpeningVersionTwentyFiveLibraryAddsGenerationRecordLifecycleAndTombstoneColumns` migration test (reproducing the exact pre-v26 schema shape directly, then verifying recycle/restore and a post-upgrade file-permanent-delete tombstone all work). `UiAssetTests` was extended with the new localization keys across `GenerationHistory`/`GenerationHistoryDetail`/`RecycleBin`'s existing per-page guards, and both generation-history pages were added to the existing dialog-role Theory now that they have their own **Recycle** confirmation panel. The plain **Cost unknown** notice added to `Generate.razor` needed no new test file — `GenerateUsesLocalizedResourcesForAllApplicationOwnedUiText`'s existing exhaustive key list was extended with `CostUnknownNotice`, since the notice is a fixed, always-visible string with no state to exercise.

## Localization verification

`UiStrings.resx` is the neutral-English resource set. `UiStrings.en-AU.resx` is a small culture-specific fixture used to verify that localized values resolve instead of falling back to resource-key names. When adding application-owned UI text, add a stable descriptive key to the neutral resource set, preserve user data and diagnostic text as formatted arguments, and update the source-level localization guard for the affected component.

## Platform builds

```powershell
dotnet build src\Mellow.SlopFactory.Gui\Mellow.SlopFactory.Gui.csproj --no-restore -f net10.0-windows10.0.22621.0
dotnet build src\Mellow.SlopFactory.Gui\Mellow.SlopFactory.Gui.csproj --no-restore -f net10.0-android
```

## Manual verification

Run the applicable device checks in the repository's [manual test matrix](../../manual_tests.md) alongside the automated suite. Record the required platform, build, and results as described there.
